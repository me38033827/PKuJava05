public class Solution {
    public boolean isAnagram(String s, String t) {
      char[] a=s.toCharArray();
      char[] b=t.toCharArray();
      Arrays.sort(a);
      Arrays.sort(b);
     // (a.valueof)e.equals(b.valueof);
     return String.valueOf(a).equals (String.valueOf(b)); 
    }
}